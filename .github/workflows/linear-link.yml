name: linear-link

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: read

jobs:
  linear-link:
    uses: braininavatgroup/brain-in-a-vat-group/.github/workflows/linear-link-reusable.yml@main
    with:
      require_changelog: false
